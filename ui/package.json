{
  "scripts": {
    "dev": "electrobun dev",
    "build": "electrobun build",
    "barrender": "swiftc -O native/render.swift -o native/barrender",
    "build:release": "swiftc -O native/render.swift -o native/barrender && RELEASE=1 electrobun build --env=stable",
    "test": "bun test"
  },
  "dependencies": {
    "electrobun": "1.18.1",
    "hono": "^4.12.27"
  }
}
